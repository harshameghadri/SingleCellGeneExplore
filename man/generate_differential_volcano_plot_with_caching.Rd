% Generated by roxygen2: do not edit by hand
% Please edit documentation in
%   R/generate_differential_volcano_plot_with_caching.R
\name{generate_differential_volcano_plot_with_caching}
\alias{generate_differential_volcano_plot_with_caching}
\title{Generate Differential Expression Volcano Plot(s) with Caching}
\usage{
generate_differential_volcano_plot_with_caching(
  seurat_obj,
  cell_type_to_analyze = NULL,
  metadataColumn = "cell.type.ident",
  disease_ident_column = "disease.ident",
  ident_1 = "PAH",
  ident_2 = "CTRL",
  assay = "RNA",
  logfc_threshold_dm = 0.25,
  min_pct = 0.1,
  test_use = "wilcox",
  genes_to_label = NULL,
  top_n_genes = 10,
  lab_font_size = 3,
  connector_arrow_type = "open",
  connector_length = 0.5,
  repel_min_segment_length = 0.1,
  repel_force = 1,
  repel_box_padding = 0.25,
  x_axis_breaks = NULL,
  legend_title = "Regulation",
  volcpath = "./VolcanoPlots_Cached",
  fc_cutoff_plot = 1,
  p_cutoff_plot = 0.05,
  color_up_regulated = "gold",
  color_down_regulated = "royalblue",
  color_non_significant = "grey",
  save_deg_results = TRUE,
  use_cache = TRUE,
  force_recompute = FALSE,
  cache_env = .GlobalEnv,
  png_width = 12,
  png_height = 9,
  png_dpi = 300,
  pdf_width = 12,
  pdf_height = 9
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{cell_type_to_analyze}{Character vector or NULL. Specifies cell type(s).}

\item{metadataColumn}{Character string. Metadata column for cell type annotations.
Default is "cell.type.ident".}

\item{disease_ident_column}{Character string. Metadata column for DE comparison identities.
Default is "disease.ident".}

\item{ident_1}{Character string. First identity group for comparison. Default is "PAH".}

\item{ident_2}{Character string. Second identity group for comparison. Default is "CTRL".}

\item{assay}{Character string. Assay for \code{FindMarkers}. Default is "RNA".}

\item{logfc_threshold_dm}{Numeric. \code{logfc.threshold} for \code{FindMarkers}. Default is 0.25.}

\item{min_pct}{Numeric. \code{min.pct} for \code{FindMarkers}. Default is 0.1.}

\item{test_use}{Character string. Test for \code{FindMarkers}. Default is "wilcox".}

\item{genes_to_label}{Character vector or NULL. Genes to label on the plot.}

\item{top_n_genes}{Integer. Number of top up/down genes to label if \code{genes_to_label} is NULL.
Default is 10.}

\item{lab_font_size}{Numeric. Font size for gene labels. Default is 3.0.}

\item{connector_arrow_type}{Character. Type of arrowhead for connectors. Default is "open".}

\item{connector_length}{Numeric. Length of the connector lines from points to labels,
interpreted in "lines" units (e.g., relative to text line height).
Passed to \code{EnhancedVolcano}'s \code{lengthConnectors} argument after conversion to a grid::unit object.
Default is 0.5.}

\item{repel_min_segment_length}{Numeric. Minimum segment length for ggrepel.
Passed to \code{EnhancedVolcano}'s \code{min.segment.length} argument. Default is 0.1.}

\item{repel_force}{Numeric. Force of repulsion (requires EnhancedVolcano >= 1.7.3). Default is 1.0.
If using an older version, this parameter will be ignored by EnhancedVolcano.}

\item{repel_box_padding}{Numeric. Padding around label bounding box (requires EnhancedVolcano >= 1.7.3). Default is 0.25.
If using an older version, this parameter will be ignored by EnhancedVolcano.}

\item{x_axis_breaks}{Numeric vector or NULL. Specifies breaks for the x-axis. Default is NULL.}

\item{legend_title}{Character string. Title for the plot legend. Default is "Regulation".}

\item{volcpath}{Character string. Base directory for saving outputs.
Default is "./VolcanoPlots_Cached".}

\item{fc_cutoff_plot}{Numeric. Absolute Log2FC cutoff for lines and coloring. Default is 1.}

\item{p_cutoff_plot}{Numeric. P-value cutoff line on plot. Default is 0.05.}

\item{color_up_regulated}{Character string. Color for up-regulated genes. Default is "gold".}

\item{color_down_regulated}{Character string. Color for down-regulated genes. Default is "royalblue".}

\item{color_non_significant}{Character string. Color for non-significant genes. Default is "grey".}

\item{save_deg_results}{Logical. Whether to save the DEG table. Default is TRUE.}

\item{use_cache}{Logical. Whether to use/save cached DE results. Default is TRUE.}

\item{force_recompute}{Logical. If TRUE, re-run \code{FindMarkers} even if cache exists. Default is FALSE.}

\item{cache_env}{Environment. Environment for caching. Default is \code{.GlobalEnv}.}

\item{png_width}{Numeric. Width for PNG plots in inches. Default is 12.}

\item{png_height}{Numeric. Height for PNG plots in inches. Default is 9.}

\item{png_dpi}{Numeric. DPI for PNG plots. Default is 300.}

\item{pdf_width}{Numeric. Width for PDF plots in inches. Default is 12.}

\item{pdf_height}{Numeric. Height for PDF plots in inches. Default is 9.}
}
\value{
Invisibly returns a list with paths to outputs and status for each cell type.
}
\description{
This function performs differential gene expression analysis using Seurat's FindMarkers
for specified cell type(s) and generates volcano plot(s). It includes a mechanism
to cache FindMarkers results in the global environment to avoid re-computation.
Plot saving attempts cowplot::save_plot, then ggplot2::ggsave, then base R devices.
Allows customization of label font size, connector arrowhead type, x-axis breaks,
legend title, and parameters for label repulsion. Uses ggplot2::theme_classic().
Note: \code{repel_force} and \code{repel_box_padding} require EnhancedVolcano version >= 1.7.3.
}
