% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_violin_plot.R
\name{create_split_violin_vioplot}
\alias{create_split_violin_vioplot}
\title{Create Split Violin Plot with vioplot Backend}
\usage{
create_split_violin_vioplot(
  seurat_obj,
  genes,
  celltype_col = "cell.type.ident",
  condition_col = "disease.ident",
  condition_colors = c(CTRL = "dodgerblue2", PAH = "firebrick2"),
  main_title = "Gene Expression by Cell Type",
  save_pdf = TRUE,
  pdf_filename = "split_violin_plot.pdf",
  plot_width = 16,
  plot_height = 12,
  max_genes_per_page = 6,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing single cell RNA-seq data}

\item{genes}{Character vector of gene names to plot}

\item{celltype_col}{Character string specifying the column name containing cell types}

\item{condition_col}{Character string specifying the column name containing conditions}

\item{condition_colors}{Named vector of colors for each condition}

\item{main_title}{Character string for the main plot title}

\item{save_pdf}{Logical. Whether to save the plot as a PDF file}

\item{pdf_filename}{Character string for PDF filename if save_pdf = TRUE}

\item{plot_width}{Numeric. Width of the plot in inches (for PDF). Default is 16}

\item{plot_height}{Numeric. Height of the plot in inches (for PDF). Default is 12}

\item{max_genes_per_page}{Integer. Maximum number of genes to plot per page. Default is 6}

\item{verbose}{Logical. Whether to print progress messages}
}
\value{
Invisible NULL (creates plot as side effect)
}
\description{
Alternative implementation using the vioplot package for creating split violin plots.
This creates base R graphics instead of ggplot2.
}
\details{
This function uses the vioplot package to create split violin plots in base R graphics.
The advantage is true split violins, but the disadvantage is less flexibility
compared to ggplot2. For many genes, the function will create multiple pages.
}
\examples{
\dontrun{
create_split_violin_vioplot(
  seurat_obj = pbmc,
  genes = c("CD3D", "CD4"),
  celltype_col = "cell.type.ident",
  condition_col = "disease.ident"
)
}

}
